package com.cbb.kotlin04

import java.io.File

/**
 * 数组
 */
fun main() {
    //创建一个数组
    val arr1 = intArrayOf(10, 20, 30)
    //list集合转array
    val toIntArray = listOf(10, 20, 30).toIntArray()

    val arrayOf = arrayOf(File("xxxx"), File("yyyy"))
}